# Task 2 — The wind value gains a bearing and an announced speed

Status: done. Worktree `firefox-weather-button-kit-panel`, branch `kit-panel-and-button`.

## What changed

`src/wind.js`

- Added `GUST_MARGIN_KNOTS = 10` beside `NOTABLE_KNOTS`, with the plan's comment about NaN being the guard.
- Added `announcedKnots({ gustKnots, knots })` beside `isNotable`. An absent `gustKnots` makes the subtraction
  NaN, which compares false, so the sustained speed wins with no explicit guard.
- `toWind` now spreads `bearingDegrees` into the returned value when `wdir` is a number, above `direction`.
  A `'VRB'` bearing and an omitted `wdir` both leave the property off.

`test/wind.test.js`

- Import line takes `announcedKnots`.
- Six new tests appended, verbatim from the plan.
- Two pre-existing whole-value `deepEqual` expectations gained `bearingDegrees` (350 for the N case, 320 for
  the gust case). The gust one was reflowed to a multi-line object by `oxfmt`, past 140 columns.

`test/observation.test.js`

- `toViewModel carries the decoded wind value rather than a description of it` gained `bearingDegrees: 320`.

`test/button.test.js`

- `the tooltip names the wind and its gust…` gained `bearingDegrees: 320` on its `painted.at(-1).wind`
  expectation. The plan named only `observation.test.js` for this fallout; `button.test.js` asserts on a whole
  wind value too and needed the same one-word change. No behavioral change — the same synthetic record
  (`wdir: 320`) now carries its degrees through the whole pipeline.

## TDD trace

- RED: `npm test -- --test-name-pattern "bearing|announcedKnots"` failed with
  `SyntaxError: The requested module '../src/wind.js' does not provide an export named 'announcedKnots'`
  (127 pass / 1 fail file). The module-level import error masks the per-case failures, which is the expected
  shape when the missing export is on the import line.
- After the module change, the whole suite reported 4 failures — all four the whole-value `deepEqual`
  expectations listed above, each diffing only by the added `bearingDegrees`. That is the fallout the plan's
  Step 4 anticipated, and confirms the new field reaches the view model and the button.
- GREEN: 146 tests, 146 pass, 0 fail.

## Gates

- `npm test` — 146 pass, 0 fail.
- `npm run format` (oxfmt over docs src test) — clean, 41 files.
- `npm run lint:js` (oxlint) — clean, no output.
- `npx stylelint src/ui.css` — not run; no CSS changed in this task.
- Pre-commit hooks (lint + secrets + docs guard) ran on the commit, not bypassed. The docs guard was satisfied
  by staging the plan file with Task 2's five step checkboxes flipped to `- [x]`.

## Concerns

None blocking.

- `announcedKnots` is exported and tested but has no caller yet; a later task in the plan consumes it. If no
  task does, it is dead code and should be deleted rather than kept.
- The NaN-as-guard idiom in `announcedKnots` is deliberate per the plan and carries its own comment, but it is
  the one place in the module where an absent field is handled by arithmetic rather than by a guard clause.
  A future reader deleting the comment loses the only explanation.
