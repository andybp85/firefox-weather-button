# Task 3 — The compass dart

Status: done. Commit `91ca02d` on `kit-panel-and-button`.

## What landed

- `src/wind-dart.js` — `dartPoints({ centre, fromDegrees, scale })` and `DIRECTIONLESS_RING`, in 64-unit face units.
- `test/wind-dart.test.js` — 4 tests.
- `docs/superpowers/plans/2026-09-03-kit-panel-and-button.md` — Task 3's five step checkboxes ticked, staged with the
  code to satisfy the `20-docs-guard` pre-commit hook. No `--no-verify`.

## TDD trace

1. Test written first.
2. Red confirmed for the right reason: `ERR_MODULE_NOT_FOUND` for `src/wind-dart.js`. No stray passes.
3. Module written from the plan's listing.
4. Green: 4/4 dart tests, then 150/150 for the whole suite.

The worst vertex reach across the 16 cardinal bearings is `hypot(14, 13)` = 19.105, matching the plan's stated 19.11 and
leaving 5.9 units of clearance before the 25-unit face edge.

## Gates

- `npm test` — 150 pass, 0 fail.
- `npm run format` — 43 files, clean. It rewrapped one `flatMap` callback in the test file past the 140-column limit;
  the committed text is the formatter's, not the plan's verbatim listing.
- `npm run lint:js` (oxlint) — clean.
- `npx stylelint src/ui.css` — not run: no CSS changed in this task.
- Global lint and secrets guards ran through `~/.claude/git-hooks` on commit and passed.

## Concerns

- `DIRECTIONLESS_RING` is exported but has no test and no consumer yet. Task 4 (the button icon) is its first caller;
  it is dead export surface until then. No lint rule objects.
- The downwind/upwind split between this dart and the panel's station-model barbs is intentional per the plan, and the
  first test's comment carries that reasoning. Nothing here enforces the panel side of the split.
