# Task 5 report — Station-model wind barbs

## Status

Done. Worktree `firefox-weather-button-kit-panel`, branch `kit-panel-and-button`.

## What landed

- `src/wind-barbs.js` — new. `windBarbs({ bearingDegrees, gustKnots, knots })` returns `{ marks, shaft? }` in the
  88-unit plot. Marks are `{ filled, gust, points }`; gust marks first, sustained on top. `shaft` is absent when
  there is no bearing.
- `test/wind-barbs.test.js` — new, 9 tests.

## Spec cross-check

Every worked number in the plan's test was re-derived from the spec's *Wind plaque* section
(`docs/superpowers/specs/2026-09-03-kit-panel-and-button-design.md`, lines 196-218) before writing the test. All
agreed; no discrepancy found this time.

- shaft r 3.6 → r 30 on the meteorological bearing, `(44 + r·sin θ, 44 − r·cos θ)`: bearing 0 gives
  `(44, 40.4) → (44, 14)`.
- NE 8 → 10 kt, one full barb at the tip: root `(65.21, 22.79)`, tip `(77.38, 26.05)` from a 12.6 length at 60°
  off the shaft, on the screen right of the outward axis (`right = (−out.y, out.x)` with y running down).
- lone half barb one slot back at r 25.5, length 6.3; SSW 15 → r 30 and r 25.5; N 38 → 40 → four barbs at
  r 30 / 25.5 / 21 / 16.5.
- pennant base 5.85 (third point at r 24.15) and the 7.65 step to the next mark (half at r 22.35).
- WNW 22 G 31 → three gust barbs then two sustained, in that draw order.
- No bearing: no `shaft`, marks stacked on the upright at `(44, 14)` and `(44, 18.5)`.
- Calm: no marks.

## Deviations from the plan's code sample

None of substance. Two mechanical differences:

- `KNOTS_PER_BARB` / `KNOTS_PER_HALF_BARB` / `KNOTS_PER_PENNANT` are declared alphabetically (objects.md /
  general.md "alphabetize wherever order is otherwise arbitrary"); the plan listed half before full.
- oxfmt kept the pennant `points:` array on one 137-column line. The formatter owns formatting.

## Gates

- `npm test` — 158 pass, 0 fail (9 of them new).
- `npm run format` — clean; `npm run format:check` implicit via a no-op second pass.
- `npm run lint:js` (oxlint) — clean.
- stylelint not run: no CSS changed.
- Pre-commit hooks (lint + secrets + docs guard) ran on the real commit; no `--no-verify`.

## Concerns

- `windBarbs` returns `marks` for a wind with no bearing laid out on a notional vertical. The consumer in Task 11
  must not draw a shaft for that case even though the marks look like a north wind — the absent `shaft` key is the
  only signal, and the direction line carries the words.
- The spec's fifth worked case (S 55 G 65) is not asserted directly; the sustained half of it is (55 → pennant plus
  half). Task 11's plaque test is the natural place for the paired gust rendering.
